# PROJECT_INDEX.md — Índice técnico leve do projeto

> Não copie código inteiro neste arquivo.  
> Use apenas referências para orientar leitura seletiva e economizar tokens.

## Última atualização

- Data: 2026-06-22
- Observação: **Pacote 26 — auditoria workspace↔org e finalização de 3 Selectors órfãos (2026-06-22):** usuário (Jean Duarte) pediu validação de risco porque o token do Codex acabou no meio do trabalho e não havia confirmação de que tudo localmente estava deployado. Método: `sf project generate manifest` a partir de todo o `force-app` local → `sf project retrieve start --manifest <gerado> --output-dir <pasta separada>` → diff de existência de arquivos (não só conteúdo, que tem muito ruído de reordenação XML). Resultado: de 1645 arquivos locais, só 4 não existiam na org — todas as 4 eram classes Apex do mesmo commit `db13c29` (17/06, "refactor"): `AtendimentoContextResolverSelector`, `CaseSurveyDispatchSelector`, `GestaoSLASelector`, `WSWillCaseCreationSelector`. Nenhuma era referenciada por código algum (confirmado via grep) — os Services correspondentes tinham SOQL inline duplicada em vez de chamar o Selector, ou seja, o refactor (padrão Selector) tinha sido iniciado mas nunca conectado/finalizado. **Religados e deployados**: `AtendimentoContextResolverSelector` (3 métodos: Contacts/Accounts por Id, ChannelRouting ativo), `CaseSurveyDispatchSelector` (6 métodos: Case/SurveySubject/Survey/EmailTemplate/OrgWideEmailAddress/Network), `WSWillCaseCreationSelector` (4 dos 6 métodos: MessagingSession/MessagingEndUser por Id, Case por Id, QueueSobject de Case — os 2 métodos de Contact foram removidos do Selector e ficaram como estavam no Service, que tem um fallback defensivo contra `ContaRepresentadaAtual__c` inexistente que o Selector não reproduzia). Criado `WSWillCaseCreationSelectorTest.cls` (cobertura direta, já que o seam `testMessagingById`/`testEndUserById` do Service sempre intercepta antes da query real, deixando 0% de cobertura na query em si). **`GestaoSLASelector` (425 linhas) permanece não religado/não deployado, deliberadamente**: `GestaoSLAService.cls` acumulou 40+ blocos de SOQL com filtros de segurança adicionados em pacotes anteriores desta mesma sessão (restrição de Unidade de Negócio visível por Record Type do usuário); nenhuma query bate exatamente com a versão genérica do Selector — religar sem checagem query-a-query arriscaria reverter essa correção de segurança. Decisão de finalizar ou descartar esse Selector específico fica pendente para sessão futura. Deploy `RunSpecifiedTests` Succeeded, 35/35 testes. **Resolução final do source tracking local (pedido explícito do usuário)**: `sf project deploy preview` estava quebrado com `TypeInferenceError` referenciando um arquivo de tradução órfão (`Categorizacao__c-en_US/PorCategorizacao__c.fieldTranslation-meta.xml`) que não existe mais localmente nem no disco — cache de tracking do CLI corrompido/desatualizado (`.sf/orgs/<orgId>/localSourceTracking` e `maxRevision.json`). Corrigido com `sf project delete tracking` (limpa só o cache local de bookkeeping, não toca na org) seguido de um retrieve completo (`sf project generate manifest` + `sf project retrieve start --manifest`) para reestabelecer a baseline. **Achado lateral do resync, confirmado seguro**: o retrieve removeu localmente 18 arquivos de campo de `ParametrosAtendimento__mdt` (`BusinessEmail__c`, `CaseOrigin__c`, `RouteKey__c`, `RouteOwnerType__c`, etc.) — são da abordagem ANTIGA de roteamento de e-mail, já migrada para o objeto dedicado `WS_EmailRoute__mdt` em sessão anterior; os campos já não existiam na org (confirmado por SOQL: `INVALID_FIELD`) e nada no Apex/Flow atual os referencia (a colisão de nomes é só porque `WS_EmailRoute__mdt` tem campos com os MESMOS nomes, e esses sim existem e são usados por `WS_EmailToCaseConfigService`). Estado final: `sf project deploy preview` mostra exatamente **1 item pendente** — `GestaoSLASelector` (a decisão deliberada de não deployar, documentada acima) — confirmando que não resta nenhum outro trabalho local não sincronizado com a org.
- Observação: **Pacote 25 — registro de última/próxima execução no motor de Agendamento Recorrente (2026-06-22):** fechando o item 0252 do checklist Triscal. Novos campos `Agendamento__c.UltimaExecucao__c` (Datetime, gravado a cada Case criado com sucesso) e `Agendamento__c.ProximaExecucao__c` (Datetime, pré-calculado e persistido a cada execução do motor — sucesso, erro, ou primeira avaliação antes de ser elegível). `CaseScheduleService.computeNextExecution()` (novo): busca, dia a dia (limite ~740 dias), a próxima data que respeita `Frequencia__c` (incluindo `SPECIFIC_WEEKDAYS`/dias específicos do Pacote 24) e `PularFimDeSemana__c`, combinada com `HorarioExecucao__c`. Em caso de sucesso, a busca começa no dia seguinte ao fim da janela de execução corrente (`buildExecutionWindow`) — ex: agendamento semanal processado hoje aponta a próxima execução para a mesma semana-dia em +7 dias, não amanhã. Em caso de erro/skip-antes-da-primeira-vez, a busca começa a partir de hoje (ou da Data de Início, se futura). Plumbing: `CaseScheduleBatch`, `AccountCaseScheduleDTO`/`Helper` (+ `ultimaExecucao`/`proximaExecucao` no `ScheduleDTO`), LWC `accountCaseScheduleManager` (exibição condicional no card). FLS: permission set `AccountCaseScheduleOperador` (read-only) + profile `Admin` (full). **Achados laterais, corrigidos (pré-existentes, não relacionados ao gap em si)**: (1) `force-app/main/default/profiles/Admin.profile-meta.xml` local estava truncado (~1750 linhas de FLS/object permissions perdidas, de ~4195 para ~2451 linhas) e com corrupção de encoding em alguns trechos — muito provavelmente atividade concorrente do Codex no mesmo workspace enquanto a sessão aguardava tokens. **A org real (`WILSON_SERVICE`) não foi afetada** — confirmado via Tooling API que o profile Admin real tem 599 `ObjectPermissions` (vs. 12 no arquivo local truncado); como deploys de `Profile` são incrementais (não revogam permissões omitidas), nada foi perdido na org. Corrigido restaurando o arquivo local via `sf project retrieve --metadata "Profile:Admin"` antes de adicionar o FLS dos 2 campos novos. (2) `CaseScheduleBatchTest.shouldProcessMultipleSchedules_WhenBatchRuns` zerava `CaseScheduleService.testNow = null` **antes** de `Test.stopTest()` — mas o batch só executa de fato durante o `stopTest()` (Salesforce roda jobs assíncronos enfileirados nesse ponto), então a hora real do servidor era usada em vez das 10h fixadas pelo teste, causando skip silencioso por "fora da janela de execução" sempre que a suíte rodasse fora de uma janela de poucas horas. Corrigido movendo o reset do `testNow` para depois do `stopTest()`. Deploy `RunSpecifiedTests` Succeeded, 45/45 testes (CaseScheduleServiceTest, CaseScheduleBatchTest, CaseScheduleSchedulerTest, AccountCaseScheduleServiceTest, AccountCaseScheduleControllerTest).
- Observação: **Email-to-Case desacoplado de `ParametrosAtendimento__mdt` e migrado para `WS_EmailRoute__mdt` (2026-06-21):** durante o fechamento da auditoria foi identificado um gap estrutural na consolidação de rotas de e-mail dentro de `ParametrosAtendimento__mdt`: a configuração geral da Unidade de Negócio tem cardinalidade `1:N` para caixas de entrada, então modelar Email-to-Case como “1 registro por unidade” criava limite artificial e risco de sobrescrita conceitual. Correção aplicada com menor risco e sem amarração ao legado: criado novo Custom Metadata Type `WS_EmailRoute__mdt` contendo **somente** os campos de rota (`UnidadeNegocio__c`, `RouteKey__c`, `BusinessEmail__c`, `SalesforceServiceAddress__c`, `MatchMode__c`, `CaseRecordTypeDeveloperName__c`, `CaseOrigin__c`, `Prioridade__c`, `RouteOwnerType__c`, `RouteOwnerDeveloperName__c`, `RouteOwnerUsername__c`, `ClosedCaseBehavior__c`, `ReopenWindowMinutes__c`, `ReopenStatus__c`, `RelateNewCaseToOriginal__c`, `IsDefaultEmailRoute__c`, `CreateContentFiles__c`, `PersistEmailAudit__c`, `AllowedFromDomains__c`, `Descricao__c`, `Ativo__c`). Quatro registros iniciais criados a partir das rotas atuais (`Tecon_Salvador`, `Tecon_Rio_Grande`, `Centro_Logistico`, `Rebocadores`). `WS_EmailToCaseConfigService` agora lê **somente** `WS_EmailRoute__mdt`; não há mais fallback para `ParametrosAtendimento__mdt`, eliminando o acoplamento ao desenho consolidado anterior. `ParametrosAtendimento__mdt` permanece para survey, branding, notificações e defaults de unidade, mas deixa de ser fonte para Email-to-Case; no saneamento final do source, os campos de rota foram removidos dele e permaneceram apenas os campos ainda usados por outras funções do parâmetro (`UnidadeNegocio__c`, `CaseRecordTypeDeveloperName__c`, `Descricao__c` e `Ativo__c`). Testes: `WS_EmailToCaseConfigServiceTest` ganhou seam de metadata (`setTestMetadataRows`) e cenário cobrindo mapeamento do novo CMDT; validate `0Afbe00000AE7rtCAD` succeeded com `43/43` testes e `WS_EmailToCaseConfigService` em `100%` de cobertura no pacote validado. Deploy real na `WILSON_SERVICE`: `0Afbe00000AE7tVCAT`.
- Observação: **Email-to-Case — fechamento dos itens parciais 0136/0153 do checklist (2026-06-21):** auditoria do módulo confirmou que a configuração de rota de e-mail **já existe em metadado versionado**, mas foi consolidada em `ParametrosAtendimento__mdt` em vez de um CMDT dedicado `WS_Email_Route__mdt`. Na correção definitiva, a configuração foi segregada em `WS_EmailRoute__mdt` e o legado de rota foi retirado de `ParametrosAtendimento__mdt`, preservando nele apenas os campos ainda usados por survey, branding, notificações, filas default e mapeamento de Record Type. Para o item 0153, a suíte foi ampliada além de `WS_EmailToCaseServiceTest`/`WS_EmailToCaseConfigServiceTest` com testes dedicados para `WS_EmailToCaseRouteService`, `WS_EmailToCaseAttachmentService` e `WS_EmailToCaseLogService`, cobrindo match por `ENVELOPE_TO`/`TO_ADDRESSES`/`CC_ADDRESSES`, ambiguidades de rota/default, fallback sem rota, publicação seletiva de anexos e persistência condicional de log técnico.
- Observação: **Fix definitivo do gap de hash em `Categorizacao__c` que travava reativação/edição (2026-06-21):** decorrente da limpeza de dados da árvore (ver observação abaixo), a tentativa de recalcular `ChaveNaturalHash__c` em massa (script `scripts/apex/recalc_categorizacao_hash.apex`) revelou que `ChaveNaturalHash__c` é **`unique=true` no metadado do campo** (constraint de banco, não só checagem em Apex) — então simplesmente filtrar `Ativo__c=true` em `CategorizacaoSelector.getExistingByHash` (primeira tentativa) não bastava: o banco bloqueia duplicidade de hash entre um registro ativo e um inativo, mesmo que o caso de negócio seja legítimo (duplicidade de carga resolvida via inativação, não exclusão). **Causa do usuário**: o saneamento das picklists globais (removendo duplicatas PT/EN, já que o projeto usa Translation Workbench) substituiu valores antigos pelos mantidos, deixando vários `Categorizacao__c` com `ChaveNaturalHash__c` desatualizado/incompatível com o conteúdo atual dos campos. **Fix aplicado em `CategorizacaoService.validateAndNormalize`** (force-app/main/default/classes/CategorizacaoService.cls): ao salvar um registro com `Ativo__c=false`, `ChaveNaturalHash__c` agora é gravado como `null` em vez de recalculado — Salesforce permite múltiplos `null` em campo `unique=true`, então um registro inativo nunca mais bloqueia um ativo (ou uma futura reativação) com a mesma chave natural. Ao reativar/salvar como ativo, o hash volta a ser calculado normalmente. `CategorizacaoSelector.getExistingByHash` também ganhou `AND Ativo__c = true` (defesa em profundidade, redundante após o fix do hash nulo, mas mantido). A checagem de duplicidade na reativação (`GestaoSLAService.reactivateCategoria` → `validateCategoriaDuplicateActive`, já existente, filtra por `Ativo__c=true` nos campos de negócio) continua sendo a guarda funcional correta nesse fluxo — confirmada com teste novo. Testes novos: `CategorizacaoServiceTest.testDuplicidadeInativaNaoBloqueiaNovaAtiva` (insere ativo, inativa, confirma hash nulo, insere novo ativo com mesma chave com sucesso) e `GestaoSLAServiceTest.testReactivateCategoriaBloqueadaPorDuplicidadeAtiva` (reativação é bloqueada quando já existe uma ativa equivalente). Deploy `0Afbe00000AE6vpCAD` (4 classes, 69/69 testes, 0 erros). **Aplicado em produção**: `a3Kbe000000D9XTEA0` (gêmeo duplicado, inativo) re-salvo para zerar o hash sob a nova lógica e depois excluído (autorizado pelo usuário, ambiente de desenvolvimento, sem Case vinculado); `a3Kbe000000D9XSEA0` (mantido) confirmado salvável novamente com hash correto. **Gap de dados separado, RESOLVIDO por exclusão (decisão do usuário)**: o recálculo em massa expôs 14 `Categorizacao__c` ativas do Rio Grande com `Categoria__c = null` (campo obrigatório) — Elogio/Incidente/Informação/Reclamação/Solicitação com Assunto "Nota Fiscal"/"Solicitação para faturar"/"PO / Draft", todas com `ExternalId__c` nulo e `CreatedDate` de 22/05 (lote anterior à carga principal de 12/06, sem rastreabilidade de origem). Sem Case vinculado a nenhuma. Como `Categoria__c` é validado incondicionalmente em `CategorizacaoService.validateAndNormalize` (independe de `Ativo__c`), elas nunca puderam ser salvas/inativadas desde que essa regra existe — única saída viável era exclusão direta (delete não passa por `beforeSave`). Excluídas em 2026-06-21. Resultado final: **858 `Categorizacao__c` ativas, 100% com `ChaveNaturalHash__c` válido e sem colisão** (confirmado por reexecução de `scripts/apex/recalc_categorizacao_hash.apex`: 858/858 OK, 0 falhas).
- Observação: **Limpeza de dados da Árvore de Atendimento executada na Org WILSON_SERVICE (2026-06-21):** ver observação detalhada mais abaixo (registros-lixo excluídos, causa raiz da duplicidade Rebocadores/Comercial identificada).
- Observação: **Pacote 24 — gap "Dias específicos da semana" fechado no motor de Agendamento Recorrente (2026-06-21):** auditoria do checklist Triscal (item 0246) encontrou que o valor de picklist "Dias específicos da semana" em `Agendamento__c.Frequencia__c` era normalizado para `WEEKLY` em `CaseScheduleService.normalizeFrequency` — ou seja, alias morto de "Semanal" (mesmo comportamento: 1 dia fixo por semana, sem suporte real a múltiplos dias). Não existia campo para guardar quais dias. Implementado: novo campo `Agendamento__c.DiasDaSemana__c` (multi-select picklist Segunda..Domingo); `CaseScheduleService` ganhou normalização própria `SPECIFIC_WEEKDAYS` (distinta de `WEEKLY`) + `matchesSpecificWeekday()` (compara dia da semana atual, via o mesmo algoritmo de `dayOfWeek()`, contra a lista `;`-separada do campo) + validação obrigando ao menos 1 dia selecionado quando essa frequência é usada; `CaseScheduleBatch` e `CaseScheduleServiceTest`/`loadSchedules` ajustados para trazer o novo campo. Plumbing completo até a UI: `AccountCaseScheduleDTO` (+`diasDaSemana` em `ScheduleDTO`/`SaveRequestDTO`, +`diasDaSemanaOptions` em `InitialStateDTO`), `AccountCaseScheduleHelper` (SOQL + mapeamento String↔List<String> via split/join por `;`), `AccountCaseScheduleService` (validação, persistência, `toSaveRequest` para edição). LWC `accountCaseScheduleManager`: `lightning-dual-listbox` condicional (`showWeekdays`) quando a frequência selecionada é "Dias específicos da semana", exibição no card do agendamento. Novos Custom Labels (`AccountCaseSchedule_Weekdays/_WeekdaysRequired/_AvailableWeekdays/_SelectedWeekdays`) em PT/EN + `fieldTranslation` do novo campo. FLS adicionado no profile `Admin` e no permission set `AccountCaseScheduleOperador`. Testes novos: `CaseScheduleServiceTest` (matching de dia específico + validação de erro sem dias selecionados) e `AccountCaseScheduleServiceTest` (save com sucesso persistindo `;`-joined + erro de validação). **Deployado e testado na WILSON_SERVICE**: deploy `0Afbe00000AE7IPCA1` Succeeded, 43/43 testes (CaseScheduleServiceTest, CaseScheduleBatchTest, CaseScheduleSchedulerTest, AccountCaseScheduleServiceTest, AccountCaseScheduleControllerTest). **Achado lateral, corrigido (flakiness pré-existente, não relacionada ao gap em si)**: `CaseScheduleServiceTest`/`CaseScheduleBatchTest` tinham quase todos os agendamentos "Diária" de teste com `PularFimDeSemana__c=true` hardcoded — como o motor corretamente pula execução em fim de semana, qualquer execução da suíte caindo num sábado/domingo real (como hoje, 2026-06-21 = domingo) fazia ~8 testes falharem (Case não criado, ou resultado "skipped" reportando `success=true` indevidamente nas asserções de erro). Corrigido trocando `PularFimDeSemana__c` para `false` nos testes que não testam especificamente esse comportamento (os 2 testes que testam o skip de fim de semana propriamente ficaram intactos, ancorados em data fixa de sábado). Também corrigido `MIXED_DML_OPERATION` pré-existente nos `@TestSetup`/helpers de `CaseScheduleServiceTest`, `CaseScheduleBatchTest` e `AccountCaseScheduleServiceTest` (inserção de `Group`/`QueueSobject`/`User` — objetos "setup" — misturada com `Account`/`Agendamento__c` na mesma transação; corrigido com `System.runAs`).
- Observação: **Limpeza de dados da Árvore de Atendimento executada na Org WILSON_SERVICE, decorrente da auditoria acima (2026-06-21):** (1) Excluídos os 2 registros-lixo (`a3Kbe000000BykfEAC` Reclamação, `a3Kbe000000BykgEAC` Informação) — só tinham `TipoCaso__c` preenchido, sem unidade/categoria/assunto/subassunto, sem Case vinculado. (2) **Causa raiz real da duplicidade Rebocadores/Comercial identificada**: os 2 registros (`a3Kbe000000D9XSEA0` ExternalId `REB_ea1fb1eb43bd4bf9` e `a3Kbe000000D9XTEA0` ExternalId `REB_2a595f799f1aa1e0`) eram 2 linhas distintas na carga original (ExternalId diferentes, prova de origem diferente), mas o campo `ChaveNatural__c` registrado neles ainda reflete um estado em **inglês** ("MISTAKE IN APPLICATION OF TAXES" vs "MISTAKE IN INSERT OF TARRIFS") enquanto os campos reais (`Categoria__c`/`Assunto__c`/`Subassunto__c`) hoje estão **byte-idênticos em português** ("Erro na aplicação de impostos") — ou seja, uma normalização/tradução PT/EN posterior (entre `CreatedDate` 12/06 e `LastModifiedDate` 20/06) igualou os 2 valores em português sem reexecutar `CategorizacaoHelper.buildNaturalKey`/recalcular `ChaveNaturalHash__c`, então o hash único não bloqueou a duplicidade resultante. **Risco sistêmico**: qualquer outra linha da árvore que tenha passado por esse mesmo tipo de atualização em lote (fora do trigger `CategorizacaoTrigger`→`CategorizacaoService`) pode ter `ChaveNaturalHash__c` desatualizado e duplicidades não detectadas — recomenda-se rodar uma rotina de recálculo em massa do hash sobre os 872 registros ativos restantes antes do próximo go-live. **Ação tomada**: `a3Kbe000000D9XTEA0` inativado (`Ativo__c=false` + `MotivoInativacao__c` documentando a causa), `a3Kbe000000D9XSEA0` mantido como canônico — nenhum dos dois tinha Case vinculado. Restam **872 `Categorizacao__c` ativos** na árvore (eram 875).
- Observação: **Auditoria da frente "Carga da Árvore de Atendimento" direto na Org WILSON_SERVICE (2026-06-21):** a planilha de checklist marcava os itens 0076-0098 como "Não iniciado", mas a árvore já está carregada (875 `Categorizacao__c`: Salvador 269, Rio Grande 476, Centro Logístico 64, Rebocadores 64, + 2 registros órfãos sem unidade). Achados reais por item: (1) **Carga em si — de fato concluída**, divergindo do status da planilha. (2) **2 registros-lixo** sem `UnidadeNegocios__c`/`Categoria__c`/`Assunto__c`/`Subassunto__c` (só `TipoCaso__c` preenchido) — candidatos a exclusão. (3) **1 duplicidade real** sobrevivendo ao hash único: Rebocadores/Incidente/Comercial/"Erro nas regras comerciais"/"Erro na aplicação de impostos" aparece 2x (hashes distintos por algum motivo — investigar `ChaveNatural__c` dessas 2 linhas). (4) **Distribuição automática (`DistribuirParaFila__c='Sim'`) configurada em só 2 de 875 linhas** — 1 real (Salvador/Portal do Cliente/Acesso → fila `TSV_AtendimentoN1`, por campo `priority=High`) e **1 registro de teste esquecido em produção** (`NomeFilaDistribuicao__c='Teste Fila Rio Grande'`, fila `TRG_Fila_2`) — remover antes de UAT. Conclusão: a "distribuição automática por categorização" está, na prática, não configurada para a árvore real (gap genuíno, não só falta de validação). (5) **Campos dinâmicos / anexo obrigatório / valor financeiro por categorização**: confirmado que `Categorizacao__c` não tem nenhum campo desse tipo, e `Case.AnexoObrigatorio__c` não é referenciado em nenhum Apex/Flow/Trigger/LWC (mesmo padrão de campo órfão do item `AreasAtendimentoAtivas__c`) — gap real, nada a validar porque a automação não existe. (6) **"Quais categorizações acionam áreas N3"**: `RegrasSLACategorizacao__c` tem 12.887 linhas (10.393 `EscopoRegra__c='Area Interna'` + 2.494 `'Atendimento'`), mas 547 das 600 categorizações com regra têm as **19 áreas cadastradas uniformemente com o mesmo SLA cada** (não é uma seleção de quais áreas essa categorização aciona — é uma tabela de "SLA se essa área for aberta manualmente para essa categorização"). Confirma a decisão já registrada no item 0237: Área Interna é sempre assistida/manual, nunca automática — então este item da planilha não é "pendente", é arquiteturalmente resolvido por essa decisão. (7) **275 das 875 categorizações (31%) não têm nenhuma `RegrasSLACategorizacao__c`** — sem SLA de Atendimento/Área Interna parametrizado se usadas; risco operacional a validar com o negócio antes do go-live de cada unidade. (8) PT/EN: nenhuma duplicidade de nomenclatura português/inglês encontrada nos 40 valores distintos de `Categoria__c` (item 0080 parece não se aplicar à carga atual). Recomendação: atualizar a planilha refletindo "Concluído" para a carga em si, manter "Não iniciado"/"Bloqueado" apenas nos sub-itens 3, 4 e 7 acima (limpeza de dados + decisão de negócio sobre cobertura de SLA), e fechar 0091-0094 como decisão arquitetural já tomada (não pendência).
- Observação: **Motor real de distribuição por campo customizado implementado (2026-06-21):** fechando a lacuna do "By Custom Field?" identificada hoje — confirmado com o usuário que a comparação deve acontecer DEPOIS do Save na tela padrão de criação do Case (não no wizard `caseNewCategorization`, que não tem acesso aos valores finais dos campos do Case). Implementado `CaseCreationService.applyCampoCustomizadoDistribution(List<Case> newRows)`, chamado por `CaseTriggerHandler.beforeInsert` (antes vazio). Lógica: para cada Case sendo inserido com `Categorizacao__c` preenchido, busca em lote (1 SOQL, bulk-safe) a Categorização; se `DistribuirParaFila__c='Sim'` + `PorCampoCustomizado__c='Sim'` + `CampoDistribuicao__c`/`ValorDistribuicao__c`/fila preenchidos, lê dinamicamente o valor atual do campo indicado no Case (`c.get(cat.CampoDistribuicao__c)`) e compara (normalizado via `CaseCreationHelper.sameNormalizedValue`) com `ValorDistribuicao__c`; se bater, sobrescreve `OwnerId` para `cat.FilaId__c` (já cacheado, sem SOQL extra) e marca `DistribuicaoSolicitada__c=true`/`OrigemDistribuicao__c='Categorização'`/`FilaDestinoDeveloperName__c`/`FilaDestinoId__c`. Roda independente da Ação de Destino escolhida no wizard (mesmo se "Assumir" foi selecionado, o Owner é sobrescrito se o critério bater). 3 testes novos em `CaseCreationServiceTest.cls` (match/não-match/categorização sem campo customizado). Gap arquitetural correlato registrado pelo usuário: Codex definiu Apex Triggers sem analisar se Flow Trigger + Apex invocável seria preferível em alguns casos — decisão pontual aqui foi manter Apex puro (consistência com o padrão já adotado). Deploy: `0Afbe00000AE6EHCA1` (20 testes, 0 erros). **Limitação que permanece, por decisão consciente**: Área Interna (N3) nunca será criada automaticamente, mesmo com esse motor — distribuição por campo customizado nunca aponta para Área Interna nos cenários reais hoje, e essa decisão (item 0237, sessão anterior) continua válida e não foi alterada por este motor.
- Observação: **Gap arquitetural Apex Trigger vs Flow Trigger+Invocable — ANALISADO E FECHADO (2026-06-21):** auditoria completa dos 5 triggers do projeto (`AreaParticipanteTrigger`, `CaseAfterInsertTrigger`, `CaseTrigger`, `CategorizacaoTrigger`, `RegrasSLACategorizacaoTrigger` — confirmado que `GestaoSLA__c`/`MarcoSLA__c`/`Agendamento__c` não têm trigger algum, são 100% Apex chamado por tela). Conclusão: o padrão Trigger→Handler→Service está corretamente aplicado; a maioria dos blocos exige Apex genuinamente (BusinessHours.add/diff para SLA com calendário comercial, Schema.describe/PicklistEntry dinâmico para nome de campo vindo de configuração em runtime, hash SHA-256 para duplicidade, CaseMilestone sem suporte a trigger nativo — por isso usa Queueable —, recursão controlada com isolamento de falha entre múltiplos objetos relacionados). Único candidato real a Flow: `RegrasSLACategorizacaoService.inheritBusinessHoursFromGestaoSLA` (linhas 35-56, herança simples de campo via lookup) — mas está na mesma transação que a validação de duplicidade (que precisa de Apex), então migrar só esse trecho não traria ganho funcional, só estético. **Decisão**: não migrar nada; as escolhas já feitas pelo Codex estão tecnicamente corretas pela natureza dos problemas, não são dívida técnica. Gap fechado, não é mais pendência.
- Responsável: Claude (Case 00435368 não distribuído — auto-sugestão de destino na criação de Case, a pedido de Jean Duarte)
- Observação: **Componente `caseNewCategorization` descoberto e recuperado (2026-06-21):** investigando o relato "Case criado deveria ter sido distribuído para fila mas ficou com meu owner", descobri que o Action Override "New" do Case aponta para o LWC `caseNewCategorization` — que **só existia na Org**, não estava no repositório local (drift, criado em 21/06 pelo próprio usuário). Recuperado via `sf project retrieve` para `force-app/main/default/lwc/caseNewCategorization`. Esse componente é um wizard de 3 passos (Contexto → Categorização Inicial → Destino do Caso) que roda ANTES da página padrão de criação do Case — ele NUNCA insere o Case diretamente; ao final, chama `CaseCreationService.buildDefaultValues` e navega para a página padrão `standard__objectPage`/`actionName=new` com `defaultFieldValues` codificados na URL (o usuário ainda precisa clicar Salvar na página nativa).
- Observação: **Causa raiz do Case 00435368 não distribuído: comportamento esperado, não bug.** O passo "3. Destino do Caso" tem 3 opções (Assumir/Distribuir/Encerrar) com **"Assumir o caso" como padrão** — `resolveCategorizationSelection` (que resolve a fila parametrizada) só era chamado quando o usuário escolhia "Distribuir" manualmente. O usuário confirmou que não mudou o destino padrão ao criar o Case — por isso ele ficou com o criador como Owner, exatamente como a tela indicava. **Corrigido**: `caseNewCategorization.js` agora chama `resolveCategorizationSelection` sempre que a árvore de categorização (Tipo/Categoria/Assunto) fica completa, independente do destino atual selecionado; se a Categorização resolvida tiver `hasParametrizedQueue=true` **e o usuário ainda não tiver escolhido manualmente** um destino (`destinationManuallySet`), o destino é auto-ajustado para "Distribuir para fila" e a fila/queue aparece pré-preenchida (`resolvedQueue`). Se o usuário mudar o destino manualmente em qualquer momento, a sugestão automática para de sobrescrever. Flag de manual-override é resetada ao trocar Record Type/Unidade. **Limitação conhecida e aceita por ora**: para Categorizações com `PorCampoCustomizado__c=true` ("By Custom Field?"), `resolveCategorizationSelection` ainda retorna `hasParametrizedQueue=false` (motor de comparação do valor real do campo do Case contra `ValorDistribuicao__c` nunca foi implementado — gap identificado em `CaseCreationService.cls:98-106` e replicado em `CaseRecategorizationService.applyDestination` linhas ~283-296; usuário decidiu não implementar agora). Para essas Categorizações específicas, a sugestão automática não dispara e o usuário ainda precisa selecionar a fila manualmente. Deploy: `0Afbe00000AE5BlCAL`.
- Responsável: Claude (pin/favorito na lista de Gestão de SLA, a pedido de Jean Duarte)
- Observação: **Pin de Gestão de SLA favorita (2026-06-21):** Adicionado `lightning-button-icon` (estilo nativo SLDS: `utility:pin`/`utility:pinned`, variant `border-filled`/`brand`) ao lado do combobox seletor de Gestão de SLA em `gestaoSLAWorkspace`. Clicar fixa/desfixa a Gestão atualmente selecionada como favorita, persistida em `window.localStorage` (chave `gestaoSLAWorkspace.pinnedGestaoId`) — decisão deliberada de não criar Custom Setting/campo no servidor, por ser uma preferência de UI de baixo impacto (favorito por navegador/usuário local, não sincronizado entre dispositivos). `resolveSelectedGestaoId()` passou a preferir a Gestão fixada (quando ainda existe na lista) ao carregar a tela, mantendo prioridade para a seleção já ativa na sessão. Sem mudança de schema/Apex. Labels novos: `GestaoSLA_PinButton`/`GestaoSLA_UnpinButton`. Deploy: `0Afbe00000AE33VCAT` (14 testes, 0 erros).
- Responsável: Claude (bug fix: edição de Categorização "perdia" os campos de distribuição ao reabrir, a pedido de Jean Duarte)
- Observação: **Bug corrigido — `GestaoSLAService.getCategorias` não retornava os campos de distribuição (2026-06-21):** Usuário reportou que, ao editar uma Categorização (Distribute to queue?/Case Queue/By Custom Field?/Which field/Value) e salvar, a tela confirmava sucesso, mas reabrir o MESMO registro mostrava os campos como se nunca tivessem sido preenchidos. Investigação com dado real (`CAT-000221` / `a3Kbe000000ByagEAC`) confirmou via SOQL direto que os valores estavam CORRETOS no banco (`DistribuirParaFila__c='Sim'`, `PorCampoCustomizado__c='Sim'`, `CampoDistribuicao__c='priority'`, `ValorDistribuicao__c='High'`, etc.) — ou seja, o save funcionava. Causa raiz: `getCategorias` (método usado para listar/recarregar Categorizações na tela, chamado após cada save) tinha sua própria construção manual de `GestaoSLADTO.CategoriaResumo` que nunca incluía os campos de distribuição (`distribuirParaFila`, `porCampoCustomizado`, `filaDeveloperName`, `nomeFilaDistribuicao`, `campoDistribuicao`, `valorDistribuicao`, `labelCampoDistribuicao`, `labelValorDistribuicao`) — diferente do helper `toCategoriaResumo` (usado por `createCategoria`/`updateCategoria`/`deactivateCategoria`), que já mapeava tudo corretamente. Isso não é um bug introduzido na sessão de hoje — é pré-existente, mas só ficou visível/testável agora que a tela passou a expor esses campos de forma mais clara (reorg + rename "By Custom Field?"). Corrigido fazendo `getCategorias` reutilizar `toCategoriaResumo(row, totalRegrasAtivas)` em vez de duplicar a construção do DTO manualmente. Confirmado: `GestaoSLADTO.CategoriaResumo` é usado SOMENTE pela tela administrativa (Gestão de SLA); a criação real de Case usa um caminho totalmente separado (`CaseCreationSelector`/`CaseCreationService`/`CaseCreationDTO`), então este bug nunca afetou a criação/distribuição de Case em produção — só a experiência de edição na tela admin. Deploy: `0Afbe00000AE2tpCAD` (79 testes, 0 erros).
- Observação: **Campo `PorCategorizacao__c` renomeado para `PorCampoCustomizado__c` com inversão de semântica (2026-06-21):** A tela de cadastro/edição de Categorização tinha o fluxo "Distribute to queue? → By categorization? → Case Queue → Which field/Value" confuso — na prática, toda distribuição é "por categorização"; o que varia é se há um critério extra de campo/valor do Case. Reordenado para "Distribute to queue? → Case Queue → By Custom Field? → Which field/Value" (toggle "By Custom Field?" nasce Inativo). Como o nome novo inverte o significado (antes: `true` = direto pela categorização, sem campo extra; agora: `true` = usa campo customizado, exige Which field/Value), foi necessário: (1) criar `PorCampoCustomizado__c` (Picklist Sim/Não, mesmo valueSet `SimNao`) e excluir `PorCategorizacao__c` via destructive deploy (`0Afbe00000AE2VdCAL`) — sem perda de dado, pois nenhuma Categorização tinha distribuição configurada ainda; (2) inverter a condição em todos os pontos de roteamento real de Case: `CaseCreationService.resolveCategorizationSelection`, `CaseRecategorizationService` (linha ~286), `CategorizacaoHelper.buildNaturalKey`/`toSObject`/`toView`, `CategorizacaoService.beforeSave`/`clearDistribution`, `GestaoSLAService.applyDistribuicaoRequest`; (3) atualizar FLS em 41 profiles + `GestaoSLAAdminTecnico` permission set (script automatizado); (4) atualizar `gestaoSLAWorkspace` (LWC) — reorder + inversão do getter `showCategoriaCampoValor` + handler `handleCategoriaInputChange`; (5) renomear Custom Label `GestaoSLA_CategoryByCategorization` → `GestaoSLA_CategoryByCustomField` ("By custom field?" / "Por campo personalizado?"). Deploy principal: `0Afbe00000AE1w9CAD` (108 testes, 0 erros).
- Observação: **Achados de drift pré-existente no Org, corrigidos durante o deploy acima (não relacionados à tarefa em si):** (a) perfil "B2BMA Integration User" com drift de permissão (`dfsle__EOS_Type__c`) que bloqueava deploy de profiles em lote — excluído deste deploy específico, drift permanece sem correção (candidato a item futuro de auditoria); (b) valores de picklist obsoletos em massa de teste (`Subassunto__c='-'` removido do org, `Assunto__c='Agendamentos'` virou `'Agendamento'`) causavam falha em `CaseRecategorizationServiceTest`/`CategorizacaoServiceTest` — corrigido substituindo por valores atualmente válidos (`'Acesso indisponível'`, `'Agendar visita presencial'`); (c) bug real em `CategorizacaoSelector.listCaseQueuesByPrefix` — não respeitava o hook de teste `testQueuesByDevName` (diferente de `getCaseQueuesByDeveloperName`, que já respeitava), causando falha em `CategorizacaoControllerTest.testSelectorQueueLookupAndExistingHash`; corrigido adicionando o mesmo hook (após o guard-clause de prefixo vazio, para não quebrar `testSelectorGuardClausesAndNullScenarios`); (d) job agendado "Wilson Sons - Motor de Agendamento de Casos - Hora em Hora" (`CaseScheduleScheduler`, cron `0 0 * * * ?`) bloqueava deploy de Apex na cadeia de `CaseCreationService`/`CaseCreationSelector` — abortado antes do deploy e reagendado com os mesmos parâmetros exatos imediatamente depois (confirmado ativo, próximo disparo 2026-06-21 20:00 UTC).
- Responsável: Claude (UX da tela de Gestão de SLA — reordenação de campos, layout em tabela para os marcos únicos, e abertura de edição de tempo para o Gestor, a pedido de Jean Duarte)
- Observação: **Reorganização da tela de criação/edição de Gestão de SLA (2026-06-21):** Static Resource movido para entre Business Hours e Status (no form de edição; no de criação, que não tem campo Status, fica logo após Business Hours). Os 3 marcos únicos (Usa Tempo de Triagem/Resposta Chat/Fila de Email + respectivo campo de tempo) movidos para depois do Status, em layout de "tabela" (`gestaoSLAWorkspace.html`/`.css`, classes `marco-unico-row`/`marco-unico-value-col`): grid de 2 colunas com 3 linhas, toggle na coluna esquerda e o campo de tempo (quando exibido) sempre alinhado na mesma margem da coluna direita — a coluna sempre ocupa o espaço, só o conteúdo é condicional. **Mudança de permissão correlata**: `canEditGestao` (gestaoSLAWorkspace.js) passou a aceitar também `permissions.canManageCategories` (persona "Gestor", Permission Set `GestaoSLAConfigurador`), não só Admin Técnico — confirmado com o usuário que o Gestor representa quem tem `GerenciarCategoriasGestaoSLA`. Campos estruturais (Nome, Unidade, Entitlement Process, Business Hours, Static Resource, Descrição) e os 3 checkboxes "Usa Tempo de X?" ficam `disabled` na UI para quem não é Admin (getter `isCamposEstruturaisReadOnly`); só os 3 campos de valor de tempo continuam editáveis para o Gestor. Blindagem replicada no servidor (`GestaoSLAService.updateGestaoSLA`): para chamadas de não-Admin, esses campos são silenciosamente forçados ao valor atual do registro antes de qualquer validação/gravação (defesa em profundidade — não depende só da UI desabilitar o campo); também foi adicionado `ensureUnidadeAllowed` (antes ausente nesse método, pois era 100% Admin-only) para impedir Gestor de uma unidade editar Gestão de outra. Label novo: `GestaoSLA_MarcosUnicosSectionTitle` ("Marcos Únicos"). Testes reescritos em `GestaoSLAServiceTest.cls`: `testUpdateGestaoSLASemNenhumaPermissaoDeEdicaoBloqueado` (sem nenhuma das 2 permissões → bloqueado), `testUpdateGestaoSLAGestorPodeAlterarTempoDeMarcoJaHabilitado` (Gestor edita tempo de marco já habilitado; tentativas de alterar Nome/Status/habilitar novo marco são ignoradas), `testUpdateGestaoSLAGestorNaoPodeAlterarStatus`. Deploy: `0Afbe00000ADyGjCAL` (53 testes, 0 erros). Criação de Gestão (`canCreateGestao`) permanece Admin-only — não alterada.
- Responsável: Claude (fechamento do item 0237 — decisão definitiva sobre criação automática de Área Interna, a pedido de Jean Duarte)
- Observação: **Decisão de negócio definitiva: Área Interna é sempre assistida (2026-06-21):** Investigação do item de checklist 0237 levou à causa raiz: `AreaParticipanteMilestoneSyncService.TIPO_BY_MILESTONE` não mapeia o Milestone nativo "Atendimento N3", com comentário no código indicando que isso era uma "decisão de negócio pendente". Decisão tomada com o usuário: confirmado que Área Interna **nunca** deve ser criada automaticamente, mesmo quando o Case nasce direto numa etapa que dispara esse Milestone — sempre exige ação manual do atendente via `AreaParticipanteController.addParticipation`. Os testes que comprovam esse comportamento (`buildRecord_atendimentoN3_naoMapeado_tipoNull`, `syncByCaseIds_milestoneNaoMapeado_naoInsere`) **já existiam** e não precisaram de alteração funcional — só os comentários (no código e nos testes) foram atualizados de "decisão pendente" para "decisão definitiva", documentando o motivo. Deploy: `0Afbe00000ADxanCAD` (22 testes, 0 erros). Decisão correlata para o item 0226 (campo `Case.AreasAtendimentoAtivas__c`, confirmado órfão na auditoria anterior): permanece sem uso por este mesmo motivo — não é uma lacuna a preencher, é uma automação que definitivamente não existirá; candidato a aposentação numa limpeza futura.
- Observação: **Auditoria da frente SLA, Entitlement Process e Área Participante (2026-06-21):** Verificação item a item dos 34 itens da frente contra código real (não apenas releitura de auditoria anterior). 3 correções: (1) `Case.AreasAtendimentoAtivas__c` — **gap confirmado**: campo existe (FLS em todos os profiles, presente nos 4 Record Types) mas não é referenciado em nenhuma classe Apex, Flow, Trigger, LWC ou critério de Milestone do Entitlement Process — busca exaustiva em todo `force-app` deu zero ocorrências fora de metadata declarativo. O "acionamento de milestones concorrentes por área" real não usa esse campo: cada `AreaParticipante__c` carrega seu próprio `AreaAtendimento__c`, com prazo calculado por `RegrasSLACategorizacao__c.AreaAtendimento__c` (linha por área, criada explicitamente via `AreaParticipanteService.addParticipation/addParticipationBulk`). Campo está órfão — avaliar se deve ser aposentado ou se falta alguma integração planejada. (2) "Criar Milestones de Atendimento de Área Interna por área N3" — confirmado que existe só 1 `MilestoneType` nativo genérico "Atendimento N3" (não um por área, como a especificação original sugeria); o rastreamento real por área é na camada Custom (`AreaParticipante__c`/`RegrasSLACategorizacao__c`), que atende ao requisito funcional por caminho diferente do literal do documento — mantido Concluído, mas com a divergência registrada. (3) "Configurar regra para pausar SLA em Em Acompanhamento" estava marcado Parcial por engano — `AreaParticipanteSLAHelper.ETAPAS_PAUSA_SLA` já é um Set genérico contendo `{'Aguardando Cliente', 'Em Acompanhamento'}`, e `CaseAreaParticipantePauseService.handleEtapaChanges` itera sobre o Set sem distinção — corrigido para Concluído. Gap secundário identificado: não há teste unitário específico para a etapa "Em Acompanhamento" (só "Aguardando Cliente" está coberto em `CaseAreaParticipantePauseServiceTest`), apesar do código já suportar — recomenda-se duplicar um cenário de teste. Demais 29 itens confirmados corretos (Business Hours, Entitlement Process v3/v4, novo marco Fila Email, criação automática de Área Participante via `AreaParticipanteMilestoneSyncService.syncByCaseIds` — confirmado genérico, sincroniza qualquer `CaseMilestone` do Case sem filtro por nome, cobrindo Categorização Inicial/Tratamento Primário/Retorno ao Cliente/SLA Total igualmente).
- Responsável: Claude (novo marco único "Fila de Email" na Gestão de SLA, a pedido de Jean Duarte)
- Observação: **Novo marco único "Fila de Email" em Gestão de SLA, generalização do motor e migração de dados (2026-06-21):** Necessidade de negócio: um novo marco de fila de e-mail, tratado como os já existentes Triagem/Resposta Chat (tempo único por Gestão de SLA, sem dependência de categorização). Descoberta: Rio Grande/Centro Logístico/Rebocadores já tinham esse tempo armazenado por engano em `TempoTriagemMinutos__c` (nunca usaram Triagem de fato); só Salvador usa os 3 marcos (Triagem + Resposta Chat + Fila de Email) simultaneamente. Pediu-se explicitamente para **não hardcodar a regra de visibilidade por unidade**. Solução adotada: `GestaoSLA__c` ganhou 3 checkboxes (`UsaTempoTriagem__c`, `UsaTempoRespostaChat__c`, `UsaTempoFilaEmail__c`) + `TempoFilaEmailMinutos__c`; o Admin decide por Gestão (não por unidade em código) quais marcos usar, direto na tela de criação/edição da Gestão de SLA (`gestaoSLAWorkspace`) — o campo numérico só aparece quando o checkbox correspondente está marcado. `GestaoSLAService.syncMarcosUnicos()` (novo) cria/reativa ou desativa automaticamente o `MarcoSLA__c` correspondente a cada toggle (nunca exclui, preserva histórico de `CaseMilestone`). `CaseMilestoneTriggerTimeCalculator.resolveTempoGestao()` ganhou mais um `else if` (padrão already existente, casamento por nome do marco — não foi necessário criar campo de mapeamento extra em `MarcoSLA__c`, pois `MilestoneTypeName__c` já é mantido em sincronia com o nome real do Milestone Type). O usuário já havia criado o `MilestoneType` "Fila Email" e as novas versões do Entitlement Process direto na Org antes de mim (`atendimento salvador_v4` com Triagem+Fila Email coexistindo; `atendimento rio grande_v3`/`atendimento centro logistico_v3`/`atendimento rebocadores_v3` com Triagem **substituído** por Fila Email) — recuperadas via `sf project retrieve start -m "MilestoneType" -m "EntitlementProcess"`. **Migração de dados executada (Apex anônimo, fora do `GestaoSLAService` deliberadamente):** Rio Grande/Centro Logístico/Rebocadores: valor de `TempoTriagemMinutos__c` (60/120/15) copiado para `TempoFilaEmailMinutos__c`, `TempoTriagemMinutos__c` zerado, `UsaTempoTriagem__c=false`/`UsaTempoRespostaChat__c=true`/`UsaTempoFilaEmail__c=true`, `MarcoSLA__c` "Triagem" renomeado para "Fila Email" (3 registros). Salvador: `UsaTempoTriagem__c=true`/`UsaTempoRespostaChat__c=true`/`UsaTempoFilaEmail__c=true`, novo `MarcoSLA__c` "Fila Email" criado (Triagem/Resposta Chat preservados intactos); `TempoFilaEmailMinutos__c` ficou intencionalmente em branco (decisão de negócio pendente) — por isso a migração foi feita via DML direto, já que a validação normal do Service (`validateTempoUnico`) exige valor sempre que o checkbox está marcado, o que é o comportamento correto para edições futuras pela tela. **Deploys executados:** código (campos, Apex, LWC, labels, translations) — `0Afbe00000ADwtFCAT` (430 componentes, 0 erros, 88 testes `RunSpecifiedTests`); ajuste de descrição de Custom Label (>80 chars, erro de validação) corrigido antes do deploy final. **Achado lateral:** ao tentar validar deploy de translations numa rodada anterior (tarefa de Categorização), havia uma referência órfã de tradução do flow `Periodo_Before_Trigger` já removida; não houve recorrência aqui. **Migração da distribuição de fila para o gestaoSLAWorkspace e descontinuação do override legado (2026-06-21):** Achado: existia um componente Aura `categorizacaoManagerOverride`/`categorizacaoViewOverride` (overrides de New/Edit/View do objeto `Categorizacao__c`) que delegava para LWCs `categorizacaoManagerV2`/`categorizacaoViewerV2` — nenhum dos dois estava versionado em `force-app` (vivia só na Org `WILSON_SERVICE`); recuperados via `sf project retrieve start -m "LightningComponentBundle:categorizacaoManagerV2,categorizacaoViewerV2"` para análise. Esse componente legado já implementava a parte que faltava no `gestaoSLAWorkspace`: listagem dinâmica de campos picklist do `Case` (`CategorizacaoController.getCasePicklistFields`), listagem de filas por unidade (`CategorizacaoController.getQueues`) e show/hide condicional dos campos de distribuição. Em vez de recriar essa lógica, ela foi **portada/reaproveitada** (sem duplicar Apex): `gestaoSLAWorkspace.js` agora importa `getCasePicklistFields`/`getQueues` diretamente de `CategorizacaoController` (classe mantida — também usada por `CategorizacaoHelper.isTrueFlag` em `CaseRecategorizationService`). `GestaoSLADTO.CategoriaRequest`/`CategoriaResumo`: +5/+8 campos de distribuição (`distribuirParaFila`, `porCategorizacao`, `filaDeveloperName`, `campoDistribuicao`, `valorDistribuicao` + labels/nome de fila no Resumo). `GestaoSLAService`: novo `applyDistribuicaoRequest()` (seta os campos a partir do request) e `queryCategoriaById()` (re-consulta pós-DML para refletir o que o trigger calculou); `createCategoria`/`updateCategoria` agora capturam `DmlException` e relançam como `FunctionalException` amigável — a validação de obrigatoriedade (fila obrigatória; campo+valor obrigatórios quando não é por categorização) **já existia** no trigger (`CategorizacaoTriggerHandler` -> `CategorizacaoService.beforeSave/validateAndNormalize`), rodando para qualquer ponto de entrada, então não foi duplicada. LWC `gestaoSLAWorkspace`: modal de Categoria ganhou toggles "Distribuir para fila?"/"Por categorização?" e combos de Fila/Campo/Valor, com show/hide replicando o padrão do componente legado. 8 novos Custom Labels (`GestaoSLA_CategoryDistributeToQueue`, `CategoryByCategorization`, `CategoryQueue(+Placeholder)`, `CategoryDistributionField(+Placeholder)`, `CategoryDistributionValue(+Placeholder)`) em PT/EN. Testes novos em `GestaoSLAServiceTest` (persistência dos campos de distribuição + 2 cenários de erro funcional amigável). **Descontinuação do componente legado:** `lwc/categorizacaoManagerV2`, `lwc/categorizacaoViewerV2`, `aura/categorizacaoManagerOverride`, `aura/categorizacaoViewOverride` excluídos do source local e da Org; `actionOverrides` de New/Edit/View em `Categorizacao__c.object-meta.xml` revertidos para `Default` (comportamento padrão do Salesforce). **DEPLOY CONCLUÍDO NA ORG WILSON_SERVICE em 2026-06-21:** (1) código novo (classes GestaoSLA*, labels, gestaoSLAWorkspace, translations) — deploy `0Afbe00000ADszJCAT`, 448 componentes, 0 erros, 65/65 testes `RunSpecifiedTests` (GestaoSLAControllerTest/GestaoSLAHelperTest/GestaoSLAServiceTest); (2) ajuste de layout do modal (toggles "Distribuir para fila?"/"Por categorização?" lado a lado, pedido do usuário após ver o resultado em produção) — deploy `0Afbe00000ADt5lCAD`; (3) `Categorizacao__c.object-meta.xml` com `actionOverrides` Default explícitos por formFactor (Large/Small) além do genérico — necessário porque remover a entrada do XML não desvincula o Action Override na Org, só sobrescrevê-la como Default o faz — deploy `0Afbe00000ADt7NCAT`; (4) exclusão destrutiva dos 4 componentes legados via `Deltas/delta_categorizacao_legacy_lwc_removal/` — deploy `0Afbe00000ADt8zCAD`, 4/4 deletados, 0 erros. **Achado lateral durante o primeiro `sf project deploy validate`:** os arquivos de tradução (`en_US.translation-meta.xml`/`pt_BR.translation-meta.xml`) tinham uma referência órfã ao flow `Periodo_Before_Trigger` (4 `customErrorMessages` de uma versão anterior do flow, incompatíveis com a versão ativa atual — o flow hoje usa elementos `customErrors`, não o schema antigo de `customErrorMessages`/`field`); isso bloqueava o deploy de QUALQUER alteração nesses arquivos de tradução. Removida a referência órfã (mantidos os 2 `textTemplates` que ainda são válidos). Pré-existente, não relacionado a esta tarefa — vale revisar se há outros flows com o mesmo problema antes do próximo deploy de translations. **Saneamento de segurança GestaoSLA por Record Type (2026-06-20):** Gap identificado por leitura de código: `GestaoSLAService.getBootstrap()` listava `GestaoSLA__c` de todas as 4 Unidades de Negócio para qualquer usuário com a Custom Permission `AcessarGestaoSLA`, sem checar o Record Type de Case disponível ao usuário — violando a segregação por unidade da especificação. Corrigido com `GestaoSLAHelper.getAllowedUnidadesNegocio()` (novo método: deriva as Unidades de Negócio permitidas a partir de `Schema.RecordTypeInfo.isAvailable()` em `Case` + mapeamento `ParametrosAtendimento__mdt.CaseRecordTypeDeveloperName__c -> UnidadeNegocio__c` via `AtendimentoConfigService`, com seam de teste `setAllowedUnidadesOverride`/`clearAllowedUnidadesOverride`). `GestaoSLAService`: novo helper privado `resolveAllowedUnidades(perms)` (retorna `null` = irrestrito para quem tem `canAdminTechnicalSettings`) e `ensureUnidadeAllowed(unidade, allowed)`, aplicados em `getBootstrap` (filtra a query + lança erro funcional se nenhuma unidade disponível), `getGestaoDetail`, `getCategorias`, `createCategoria`, `updateCategoria`, `deactivateCategoria`, `getInactiveCategorias` e `reactivateCategoria`. `GestaoSLADTO.BootstrapResponse`: +`unidadesDisponiveis` (List<String>, null=irrestrito) e +`unidadeUnica` (String, preenchido quando há só 1 opção). LWC `gestaoSLAWorkspace` não precisou de alteração: o seletor de Gestão já era condicionado a `canAdminTechnicalSettings` e `resolveSelectedGestaoId()` já auto-seleciona `gestoes[0]` — com a lista agora corretamente filtrada no backend, a auto-seleção passa a ser segura. Testes novos: `GestaoSLAHelperTest` (seam de override) e `GestaoSLAServiceTest` (bootstrap restrito a 1 unidade, admin irrestrito, sem unidade disponível lança erro, createCategoria/getCategorias bloqueados para unidade não permitida); todos os testes pré-existentes que chamam `getBootstrap/getGestaoDetail/getCategorias/createCategoria/updateCategoria/deactivateCategoria/reactivateCategoria` foram ajustados para chamar `GestaoSLAHelper.setAllowedUnidadesOverride(null)` junto do `clearPermissionOverrides()`, preservando o comportamento irrestrito que já assumiam. **Pendência:** não deployado nem testado em Org nesta sessão (sem sf CLI conectado) — rodar `sf project deploy validate` e a suíte de testes antes do merge. Achado correlato (registrado, não implementado): Permission Sets hoje não são segregados por Unidade de Negócio (apenas `GestaoSLAConfigurador`/`GestaoSLAAdminTecnico`/`CaseAcompanhamentoOperador`/`AccountCaseScheduleOperador`, todos globais) — proposta de saneamento (2 PS por unidade: Gestor/Atendente + 1 PS global de Admin Técnico) registrada na planilha de checklist, pendente de desenho/implementação. **Retrieve Marllon 17/06 (2026-06-18):** trazido para o working tree (ainda não commitado/revisado) tudo que o usuário Marllon Nascimento criou/alterou na `WILSON_SERVICE` em 2026-06-17, apurado via `SetupAuditTrail`. Novo módulo "Acompanhamento de Case": objeto custom `Acao__c` (registra início/fim, tipo, status, motivo de pausa, comentários de entrada/saída, duração calculada) relacionado a `Case`; campos novos em `Case` (`AcaoPausaAtiva__c`, `DataInicioAcompanhamento__c`, `EtapaAnteriorAcompanhamento__c`, `MotivoAcompanhamento__c`, `UltimoComentarioAcompanhamento__c`, `UsuarioInicioAcompanhamento__c`); classes `CaseAcompanhamentoController/DTO/Service(+Test)`; LWCs `caseAcompanhamentoAction`, `caseAcompanhamentoBanner`, `caseAcompanhamentoRefresh`; Quick Action `Colocar_em_Acompanhamento` no Case; Permission Set `CaseAcompanhamentoOperador`; FLS ajustado no profile `Admin`. Também trazidas: módulo de notificação de abertura de Case (`CaseOpeningNotificationService/ServiceAgent(+Test)`, chamado por `CaseAfterInsertTriggerHandler`), 3 novos campos em `ParametrosAtendimento__mdt` (`SendCaseOpeningEmail__c`, `CaseOpeningTemplateDeveloperName__c`, `CaseOpeningTemplateDeveloperNameEN__c`) com respectivo layout, e ajustes pontuais em `CaseClosureSurvey*`, `AtendimentoConfig*`, `CaseSurveyDispatchHelper/ServiceAgent`. Manifest usado: `manifest/retrieve-marllon-20260617.xml`. **Pendências de Omni-Channel (não retrieváveis/aplicáveis aqui, registradas só para contexto):** Marllon também criou/ajustou em 17/06 `QueueRoutingConfig Service_Routing_Case_N1`, `ServicePresenceStatus Caso_Disponivel`/`Caso_Messaging_Online`, renomeou o `ServiceChannel Email` para `Case`/`Caso`, e ajustou membership da fila `Atendimento N1 - Centro Logístico` — configuração viva na org, não capturada em metadata local. **Pacote 24 (2026-06-15):** Serviço read-only de auditoria de inconsistências de SLA. Novas classes: `GestaoSLAHealthCheckDTO` (DTO HealthCheckResult+Issue), `GestaoSLAHealthCheckService` (4 checks, 7 SOQL, 0 DML, `with sharing`), `GestaoSLAHealthCheckServiceTest` (16 testes). Regras detectadas: REGRA_SEM_BUSINESS_HOURS, ESCOPO_LEGADO, REGRA_DUPLICADA, REGRA_SEM_PRAZO (checkRegrasSLA); CUSTOM_ABERTA_SEM_PRAZO, SEM_STATUS_ATUACAO, SEM_STATUS_SLA, STATUS_SLA_INCOERENTE_*, VIOLOU_SLA_INCONSISTENTE_*, PAUSADO_SEM_DATA_INICIO_PAUSA, DATA_INICIO_PAUSA_INDEVIDA, TEMPO_PAUSADO_NEGATIVO (checkAreaParticipante); STANDARD_COM_PAUSA, STANDARD_SEM_MILESTONE, CUSTOM_COM_MILESTONE (checkStandardVsCustom); CASE_TERMINAL_COM_AREA_ABERTA (checkCaseEtapa). Delta: `delta_gestao_sla_health_check/`. Dry-run `0Afbe00000AAH8HCAX` Succeeded, 16/16 testes. **Pacote 22 ajustado (2026-06-15):** Hardcoded Ids removidos; teste de retomada de área vencida adicionado; sincronização delta↔force-app; dry-run `0Afbe00000AAAJhCAP` Succeeded, 40/40 testes. **Pacote 23 ajustado (2026-06-14):** `canPause` e `canResume` passaram a ser calculados no backend (`AreaParticipanteService` -> `AreaParticipanteDTO`) considerando `TipoAreaParticipante__c = Area Interna`, `OrigemSLA__c = Custom`, estado terminal e estado de pausa; o LWC `caseAreasParticipantesPanel` apenas renderiza as flags. O teste Jest do componente foi incluido no delta como artefato de apoio em `tests/lwc/...`. `package.xml` alinhado para `66.0`. Dry-run corrigido `0Afbe00000AAAd3CAH` Succeeded, 40/40 testes. **Pacote 22 (2026-06-14):** Pausa/retomada operacional de `AreaParticipante__c` para Area Interna Custom manual via `AreaParticipanteService` e `AreaParticipanteController`; sem criar Flow, Trigger, LWC, campo, layout ou Permission Set. Pausa usa `StatusSLA__c = 'Pausado'` e `DataHoraInicioPausa__c`, preservando `StatusAtuacao__c` porque nao existe valor oficial `Pausada`. Retomada acumula `TempoPausadoMinutos__c`, estende `DataHoraPrazo__c` e recalcula cache SLA. `OrigemSLA__c = Standard` e registros terminais sao rejeitados antes de DML. Delta: `delta_area_participante_pausa_retomada/`. Dry-run `0Afbe00000AA5NGCA1` Succeeded, 39/39 testes. **Pacote 21 v3 (2026-06-14):** Orquestração de EtapaAtendimento__c no ciclo de vida das Áreas Internas. `AreaParticipanteSelector`: removido IsClosed de getCaseById() (Status já existia); +OrigemSLA__c em getAreasByCase() e getAreaById(). `AreaParticipanteService.addParticipation()` e `closeParticipation()`: guard terminal usa `statusNorm.contains('fechad')` (Status) em vez de IsClosed; loop de contagem usa `OrigemSLA__c = 'Custom'` explicitamente em vez do proxy BloqueiaFechamentoCaso__c. `AreaParticipanteServiceTest`: teste de Case fechado usa `new Case(Status='Fechado')` sem JSON.deserialize. Deploy definitivo `0Afbe00000AA4PZCA1` Succeeded, 20/20 testes, Selector 97%, Service 85%. **Pacote 20 (2026-06-14):** Bloqueio de fechamento/cancelamento de Case com Área Interna aberta. `CaseTriggerHandler.beforeUpdate`: + detecção `EtapaAtendimento__c = 'Cancelado'`; + filtro `OrigemSLA__c = 'Custom'` na query; + mensagem funcional. `CaseTriggerHandlerTest`: 7 cenários (8 total). Dry-run `0Afbe00000AA0fJCAT` Succeeded, 12/12 testes, 96% cobertura. **Pacote 19 (2026-06-14):** Sincronização de CaseMilestones para AreaParticipante__c — 3 novas classes (Service, Batch, Scheduler) + 2 testes. AreaParticipanteSLAHelper: +ORIGEM_SLA_STANDARD. AreaParticipanteSLAService: guards Standard em beforeSave/closeSLA + fix NPE em MAX(SequenciaAcionamento__c). Dry-run 0Afbe00000AA05pCAD Succeeded, 45/45 testes. Delta: delta_area_participante_milestone_sync/. **Pacote 18 reformulado (2026-06-14):** Ajustado LWC existente `caseAreasParticipantesPanel` — componente real na `LP_Atendimento_Salvador`. Adicionados getter `canManage`, botão Atualizar e `masterLabel`. LWC `caseAreaParticipantePanel` (criado no Pacote 18 original) descartado e excluído do source. Delta: `Deltas/delta_area_participante_case_component_existing_fix/`. Dry-run `0Afbe00000A9ydVCAR` Succeeded (Changed 4 arquivos). **Cleanup RegrasSLA (2026-06-14):** Removido `MarcoSLA__c` de regras `Area Interna` em 6 classes de teste (RegrasSLA x5 + CategorizacaoServiceTest). Corrigido `SLACoverageCoreTest.testAreaLifecyclePauseResumeCloseAndBlockCaseClose` e `testBatchAndSchedulerAndGuard` para definir `Categorizacao.GestaoSLA__c` antes de inserir `AreaParticipante__c` (bug latente pós-17A v2). `testFindActiveRulesNovoN3Coverage` ajustado para assertar 0 resultados (método legado não retorna Area Interna sem MarcoSLA). Deploy `0Afbe00000A9x37CAB` Succeeded. 33/33 testes. Nota: `CategorizacaoServiceTest` ainda tem 2 ocorrências da violação (fora do escopo). **Cleanup 17A (2026-06-14):** Removido `createRegraLegada` (dead code) de `AreaParticipanteSLAServiceTest`. Removido `TipoAreaParticipante__c` desnecessário de `RegrasSLACategorizacao__c` em 3 test factories. Corrigido `CaseAreaParticipantePauseServiceTest.createRule` para incluir `Categorizacao.GestaoSLA__c` e remover `MarcoSLA__c` legado. Deploy `0Afbe00000A9wNBCAZ` Succeeded. 49/49 testes. **Pacote 17A v2 (2026-06-14):** SequenciaAcionamento__c agora é global por Case (não mais por Case+Area). findRule() e getEligibleAreaValuesForCase() incluem filtro explícito GestaoSLA__c via getSObject('Categorizacao__r'). Factory usa shared GestaoSLA__c para todos os registros do mesmo Case. +1 teste (testSequenciaGlobalPorCaseNaoPorArea). Deploy `0Afbe00000A9wAHCAZ` Succeeded. 43/43 testes. **17A v1 (2026-06-14):** Deploy `0Afbe00000A9vnhCAB` Succeeded. 42/42 testes. **16B (2026-06-13):** campos legados excluídos fisicamente da org via destructiveChanges — dry-run `0Afbe00000A9v9NCAR` (Succeeded) e deploy real `0Afbe00000A9vAzCAJ` (Succeeded). Campos excluídos: `RegrasSLACategorizacao__c.Origem__c`, `RegrasSLACategorizacao__c.VigenciaInicio__c`, `RegrasSLACategorizacao__c.VigenciaFim__c`, `AreaParticipante__c.TipoAtuacao__c`. Regra: `@last modified by` sempre nome do usuário Salesforce da org (nunca nome de IA).

---

## Estrutura principal

```text
force-app/
manifest/
```

---

## Arquitetura identificada

```text
LWC / Visualforce / Flow / API
        ↓
Controller / FlowAction
        ↓
Service
        ↓
ServiceAgent / Helper / Selector
        ↓
Sistema externo / SObject / Metadata
```

---

## Controllers / FlowActions

| Artefato | Caminho | Responsabilidade | Chama |
|---|---|---|---|
| AreaParticipanteController | force-app/main/default/classes/AreaParticipanteController.cls | Expõe endpoints @AuraEnabled para AreaParticipante__c: getPanelDataFresh, addParticipation, addParticipationBulk, closeParticipation, pauseParticipation, resumeParticipation, getParticipationDetails. Sem lógica de negócio. | AreaParticipanteService |
| CaseAcompanhamentoController | force-app/main/default/classes/CaseAcompanhamentoController.cls | Expõe endpoints @AuraEnabled para o módulo Ação/Acompanhamento de Case (criado por Marllon, retrieve 17/06/2026). | CaseAcompanhamentoService |

## Services

| Artefato | Caminho | Responsabilidade | Chamado por |
|---|---|---|---|
| AreaParticipanteService | force-app/main/default/classes/AreaParticipanteService.cls | Lógica de negócio de AreaParticipante__c: adição unitária (addParticipation) e bulk (addParticipationBulk), encerramento, pausa, retomada e leitura de painel. Valida CRUD/FLS, regras SLA, unicidade de ciclo aberto. | AreaParticipanteSLAService, AreaParticipanteSelector, BusinessHoursResolverService |
| CaseAreaParticipantePauseService | force-app/main/default/classes/CaseAreaParticipantePauseService.cls | Pausa/retoma AreaParticipante interna ao entrar/sair de "Aguardando Cliente"; registra DataHoraPausaMilestone__c no Case para rastreamento de pausa dos marcos nativos. | CaseTriggerHandler.afterUpdate |
| CaseMilestoneMacroService | force-app/main/default/classes/CaseMilestoneMacroService.cls | Fecha marcos SLA não-SLA Total em qualquer transição de EtapaAtendimento__c; fecha SLA Total na conclusão/cancelamento do Case. | CaseTriggerHandler.afterUpdate |
| AreaParticipanteMilestoneSyncService | force-app/main/default/classes/AreaParticipanteMilestoneSyncService.cls | Espelha CaseMilestones para AreaParticipante__c (OrigemSLA__c='Standard'); idempotência via CaseMilestoneId__c; preserva Custom. Pacote 19. | AreaParticipanteMilestoneSyncBatch, CaseMilestoneSyncQueueable |
| CaseAfterInsertTriggerHandler | force-app/main/default/classes/CaseAfterInsertTriggerHandler.cls | Handler do CaseAfterInsertTrigger; ao inserir um Case, enfileira CaseMilestoneSyncQueueable para sincronizar CaseMilestones gerados pelo EntitlementProcess; também dispara CaseOpeningNotificationServiceAgent (ajuste de Marllon, retrieve 17/06/2026). | CaseAfterInsertTrigger |
| CaseAcompanhamentoService | force-app/main/default/classes/CaseAcompanhamentoService.cls | Lógica de negócio do objeto `Acao__c` (início/fim de ação, pausa, comentários de entrada/saída) vinculada ao Case via Quick Action `Colocar_em_Acompanhamento`. Criado por Marllon, retrieve 17/06/2026. | CaseAcompanhamentoController |
| CaseOpeningNotificationService | force-app/main/default/classes/CaseOpeningNotificationService.cls | Envia e-mail de abertura de Case conforme `ParametrosAtendimento__mdt.SendCaseOpeningEmail__c`/`CaseOpeningTemplateDeveloperName__c`(EN). Criado por Marllon, retrieve 17/06/2026. | CaseOpeningNotificationServiceAgent |
| CaseOpeningNotificationServiceAgent | force-app/main/default/classes/CaseOpeningNotificationServiceAgent.cls | ServiceAgent acionado pelo CaseAfterInsertTriggerHandler para disparar a notificação de abertura. Criado por Marllon, retrieve 17/06/2026. | CaseAfterInsertTriggerHandler |
| CaseMilestoneSyncQueueable | force-app/main/default/classes/CaseMilestoneSyncQueueable.cls | Queueable que chama AreaParticipanteMilestoneSyncService.syncByCaseIds(); executa fora da transação para aguardar CaseMilestones do EntitlementProcess. Pacote 24A. | CaseAfterInsertTriggerHandler |
| AreaParticipanteMilestoneSyncBatch | force-app/main/default/classes/AreaParticipanteMilestoneSyncBatch.cls | Apex Batch que processa Cases em lote de 200 chamando syncByCaseIds; suporta filtro por Set<Id> ou QueryLocator global. Pacote 19. | AreaParticipanteMilestoneSyncScheduler |
| AreaParticipanteMilestoneSyncScheduler | force-app/main/default/classes/AreaParticipanteMilestoneSyncScheduler.cls | Schedulable que agenda AreaParticipanteMilestoneSyncBatch periodicamente (ex: daily às 02h). Pacote 19. | — |

## Selectors

| Artefato | Caminho | SObjects cobertos | Chamado por |
|---|---|---|---|
| AreaParticipanteSelector | force-app/main/default/classes/AreaParticipanteSelector.cls | AreaParticipante__c | AreaParticipanteService |
| GestaoSLASelector | force-app/main/default/classes/GestaoSLASelector.cls | GestaoSLA__c, Categorizacao__c, MarcoSLA__c, RegrasSLACategorizacao__c — 26 métodos estáticos | GestaoSLAService |
| CaseSurveyDispatchSelector | force-app/main/default/classes/CaseSurveyDispatchSelector.cls | Case, SurveySubject, Survey, EmailTemplate, OrgWideEmailAddress, Network | CaseSurveyDispatchService |
| WSWillCaseCreationSelector | force-app/main/default/classes/WSWillCaseCreationSelector.cls | Contact, MessagingSession, MessagingEndUser, Case, QueueSobject — 6 métodos; corrige SOQL-in-loop do insert de Cases | WSWillCaseCreationService |
| AtendimentoContextResolverSelector | force-app/main/default/classes/AtendimentoContextResolverSelector.cls | Contact, Account, TCO_ChannelRouting__mdt — 3 métodos estáticos; SOQLs dinâmicos (ConversationGateway, fetchContactsByEndUser) permanecem no Service por necessidade de schema runtime | AtendimentoContextResolverService |

## ServiceAgents / Integrações

| Artefato | Caminho | API/Sistema | Named Credential | Custom Metadata |
|---|---|---|---|---|

## DTOs / Wrappers

| Artefato | Caminho | Uso |
|---|---|---|
| AreaParticipanteDTO | force-app/main/default/classes/AreaParticipanteDTO.cls | DTOs do módulo AreaParticipante: PanelDTO, AreaItemDTO, AddRequestDTO, AddBulkRequestDTO/ItemDTO/ResponseDTO, CloseRequestDTO/ResponseDTO, PauseResumeRequestDTO/ResponseDTO, DetailDTO. |

## LWCs

| Componente | Caminho | Apex usado | Responsabilidade |
|---|---|---|---|
| caseAreasParticipantesPanel | force-app/main/default/lwc/caseAreasParticipantesPanel/ | AreaParticipanteController (getPanelDataFresh, addParticipationBulk, closeParticipation, pauseParticipation, resumeParticipation, getParticipationDetails) | Painel de Áreas Participantes na LP_Atendimento_Salvador: lista agrupada Aberto/Concluído, modal wizard 2 passos (dual-listbox de seleção + comentário por área) para adicionar múltiplas áreas, encerramento, pausa/retomada, detalhes, bilíngue PT+EN, SLA. |
| caseAcompanhamentoAction / caseAcompanhamentoBanner / caseAcompanhamentoRefresh | force-app/main/default/lwc/caseAcompanhamento*/ | CaseAcompanhamentoController | Trio de LWCs na LP_Atendimento_Salvador para colocar o Case "em Espera" (Quick Action `Colocar_em_Acompanhamento`), exibir banner de status e permitir refresh manual. Criado por Marllon, retrieve 17/06/2026. |
| caseClosureSurvey | force-app/main/default/lwc/caseClosureSurvey/ | CaseClosureSurveyController | Pesquisa de satisfação no encerramento do Case; ajustado por Marllon em 17/06/2026 (sincronizado via retrieve, sem alteração de Claude/Codex). |
| gestaoSLAWorkspace | force-app/main/default/lwc/gestaoSLAWorkspace/ | GestaoSLAController | Workspace administrativo de GestaoSLA — gerencia Categorias e Regras SLA; usado em AppPage/Tab. |

## Flows

| Flow | Caminho | Objeto | Tipo | Before/After | Observação |
|---|---|---|---|---|---|
| Case_EntitlementAutoAssignment | force-app/main/default/flows/Case_EntitlementAutoAssignment.flow-meta.xml | Case | Record-Triggered | Before Save — Create | Busca Entitlement WHERE UnidadeNegocio__c = Case.UnidadeNegocio__c (LIMIT 1) e atribui EntitlementId. Substitui o Apex CaseEntitlementAssignmentService.assignForSalvador, cobrindo todas as 4 unidades. Ativo. |

## Objetos / Campos relevantes

| Objeto | Campo/Metadata | Caminho | Uso |
|---|---|---|---|
| EntitlementProcess | atendimento salvador_v2 | force-app/main/default/entitlementProcesses/atendimento salvador_v2.entitlementProcess-meta.xml | Processo ativo/default v2 de Atendimento Salvador para Case; referencia Business Hours `Atendimento Salvador` e milestones de SLA. |
| EntitlementProcess | atendimento rio grande_v2 | force-app/main/default/entitlementProcesses/atendimento rio grande_v2.entitlementProcess-meta.xml | Processo ativo/default v2 de Atendimento Rio Grande para Case; referencia Business Hours `Atendimento Rio Grande` (a criar na org) e milestones de SLA. |
| EntitlementProcess | atendimento centro logistico_v2 | force-app/main/default/entitlementProcesses/atendimento centro logistico_v2.entitlementProcess-meta.xml | Processo ativo/default v2 de Atendimento Centro Logístico para Case; referencia Business Hours `Atendimento Centro Logístico` (a criar na org) e milestones de SLA. |
| EntitlementProcess | atendimento rebocadores_v2 | force-app/main/default/entitlementProcesses/atendimento rebocadores_v2.entitlementProcess-meta.xml | Processo ativo/default v2 de Atendimento Rebocadores para Case; referencia Business Hours `Atendimento Rebocadores` (a criar na org) e milestones de SLA. |
| MilestoneType | Acompanhamento; Atendimento; Atendimento N3; Primeira Resposta (Fila N2); Resposta Chat; Retorno N3; SLA Total; Triagem | force-app/main/default/milestoneTypes/ | Milestones usados pelo Entitlement Process `atendimento salvador_v2`. |
| Settings | BusinessHours | force-app/main/default/settings/BusinessHours.settings-meta.xml | Settings org-level de Business Hours; inclui `Atendimento Salvador`, além de outros horários retornados pela Metadata API. |
| Settings | Entitlement | force-app/main/default/settings/Entitlement.settings-meta.xml | Settings org-level necessários para Entitlement Management. |
| Case | DataHoraPausaMilestone__c (DateTime) | force-app/main/default/objects/Case/fields/DataHoraPausaMilestone__c.field-meta.xml | Registra início da pausa dos marcos SLA nativos ao entrar em "Aguardando Cliente"; limpo ao sair. Usado por CaseAreaParticipantePauseService. FLS Admin: editable+readable. |
| Acao__c | Objeto custom (Case__c, DataInicio__c, DataFim__c, Status__c, Tipo__c, MotivoPausa__c, SlaPausado__c, EtapaAnterior__c, ComentarioEntrada__c, ComentarioSaida__c, UsuarioInicio__c, UsuarioFim__c, DuracaoMinutos__c fórmula) | force-app/main/default/objects/Acao__c/ | Registro de ações/acompanhamento atrelado ao Case (módulo "Colocar em Espera"). Criado por Marllon, retrieve 17/06/2026. |
| Case | AcaoPausaAtiva__c, DataInicioAcompanhamento__c, EtapaAnteriorAcompanhamento__c, MotivoAcompanhamento__c, UltimoComentarioAcompanhamento__c, UsuarioInicioAcompanhamento__c | force-app/main/default/objects/Case/fields/ | Espelham o estado da Ação ativa de acompanhamento no próprio Case. Criado por Marllon, retrieve 17/06/2026. |
| ParametrosAtendimento__mdt | SendCaseOpeningEmail__c, CaseOpeningTemplateDeveloperName__c, CaseOpeningTemplateDeveloperNameEN__c | force-app/main/default/objects/ParametrosAtendimento__mdt/fields/ | Controlam se/qual template de e-mail é enviado na abertura do Case (CaseOpeningNotificationService). Habilitado em 17/06/2026 nos registros Tecon Salvador, Tecon Rio Grande, Rebocadores e Centro Logístico. |
| Agendamento__c | DiasDaSemana__c (multi-select picklist: Segunda..Domingo) | force-app/main/default/objects/Agendamento__c/fields/DiasDaSemana__c.field-meta.xml | Dias da semana selecionados quando Frequencia__c = "Dias específicos da semana"; antes desse campo (2026-06-21) esse valor de Frequencia__c era um alias morto de "Semanal" no motor (CaseScheduleService), sem campo para guardar múltiplos dias. |
| Agendamento__c | UltimaExecucao__c (Datetime) | force-app/main/default/objects/Agendamento__c/fields/UltimaExecucao__c.field-meta.xml | Gravado pelo motor (CaseScheduleService.buildSuccessUpdate) sempre que um Case é criado com sucesso a partir do agendamento. |
| Agendamento__c | ProximaExecucao__c (Datetime) | force-app/main/default/objects/Agendamento__c/fields/ProximaExecucao__c.field-meta.xml | Pré-calculado pelo motor (CaseScheduleService.computeNextExecution) a cada execução — sucesso, erro, ou primeira avaliação — considerando Frequencia__c/DiasDaSemana__c/PularFimDeSemana__c/HorarioExecucao__c. |

## Layouts / FlexiPages

| Artefato | Caminho | Objeto | Ativação/uso |
|---|---|---|---|

## Permission Sets / Profiles

| Artefato | Caminho | Finalidade |
|---|---|---|
| Profiles (`force-app/main/default/profiles/`) | force-app/main/default/profiles/ | Fonte local dos perfis recuperados da `WILSON_SERVICE`; FLS de `AreaParticipante__c` padronizado para `Admin` com acesso total e demais perfis sem acesso aos 32 campos locais. Exceção pendente: `B2BMA Integration User`, bloqueado por licença gerenciada ao redeployar o profile completo. |
| Admin | force-app/main/default/profiles/Admin.profile-meta.xml | Perfil administrativo com `readable/editable` ativo em todos os 32 campos locais de `AreaParticipante__c`, após normalização de FLS. |

## Testes

| Classe de teste | Caminho | Cobre |
|---|---|---|

## Histórico de pacotes relevantes

| Pacote | Escopo | Campos/Constantes removidos |
|---|---|---|
| 20 | Bloqueio de fechamento/cancelamento de Case com Área Interna aberta | — |
| 15D | Refatoração de escopos | `ESCOPO_GLOBAL`, `ESCOPO_POR_CATEGORIZACAO`, `ESCOPO_POR_AREA_INTERNA` renomeados para `ESCOPO_ATENDIMENTO` e `ESCOPO_AREA_INTERNA` em `RegrasSLACompatibilidadeService` |
| 16A | Remoção de referências de campos legados | `Origem__c`, `VigenciaInicio__c`, `VigenciaFim__c` de `RegrasSLACategorizacao__c`; `TipoAtuacao__c` de `AreaParticipante__c` — referências removidas do código/metadado |
| 16B | Exclusão física dos campos | `RegrasSLACategorizacao__c.Origem__c`, `VigenciaInicio__c`, `VigenciaFim__c`; `AreaParticipante__c.TipoAtuacao__c` — excluídos via destructiveChanges. Deploy `0Afbe00000A9vAzCAJ` Succeeded. |

## Fluxos técnicos principais

### Fluxo 1

```text
Entrada:
Camadas:
Saída:
Riscos:
```

### SLA Atendimento Salvador v2

```text
Entrada: Case associado ao Entitlement Process ativo/default `atendimento salvador_v2`.
Camadas: EntitlementProcess -> MilestoneType -> ApexClass CaseMilestoneTriggerTimeCalculator.
Saída: Milestones de triagem, resposta, atendimento, acompanhamento e SLA total aplicados ao Case.
Riscos: BusinessHours é recuperado como Settings org-level; revisar escopo antes de deploy para não sobrescrever horários não relacionados.
```
